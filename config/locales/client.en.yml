en:
  js:
    rss_polling:
      title: RSS Polling

    admin:
      rss_polling:
        author: Author
        feed_url: Feed URL
        remember_to_add_allowed_hosts: Remember to add them as Allowed Hosts in "Customize > Embedding"
